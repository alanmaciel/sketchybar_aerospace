#!/usr/bin/env bash

CONFIG_DIR="${CONFIG_DIR:-$HOME/.config/sketchybar}"
PLUGIN_DIR="$CONFIG_DIR/plugins"

source "$CONFIG_DIR/themes.sh"


########################################
# BAR
########################################
sketchybar --bar \
  position=top \
  height=32 \
  blur_radius=30 \
  color="$BAR_BG"
  # border_color="$BAR_BORDER" \
  # border_width=1

########################################
# DEFAULTS
########################################
default=(
  icon.font="Hack Nerd Font:Bold:17.0"
  label.font="Hack Nerd Font:Bold:14.0"
  icon.color="$BAR_FG"
  label.color="$BAR_FG"
  padding_left=4
  padding_right=4
  label.padding_left=4
  label.padding_right=4
)
sketchybar --default "${default[@]}"

########################################
# AEROSPACE EVENT
########################################
sketchybar --add event aerospace_workspace_change
sketchybar --add event front_app_focus_changed
sketchybar --add event aerospace_window_change

########################################
# LAPTOP ROW (1–3) → display 1, monitor 2
########################################

# Must match MAX_SLOTS in workspace_pills.sh. Bump if you regularly have
# more windows open in a single workspace.
WORKSPACE_MAX_WINDOWS=10

SPACE_MON1=("1" "2" "3")

# Each pill is a bracket merging a "num" item (the workspace digit) with a
# fixed pool of "slot" items (one per potential window, up to
# WORKSPACE_MAX_WINDOWS). workspace_pills.sh (a single controller item, see
# below) fills in one slot per window with that app's icon and colors the
# focused window's icon differently, so the bracket's members render as a
# single pill.
for sid in "${SPACE_MON1[@]}"; do
  sketchybar --add item "space1.$sid.num" left \
    --set "space1.$sid.num" \
      associated_display=1 \
      icon="$sid" \
      icon.padding_left=10 \
      icon.padding_right=10 \
      label.drawing=off \
      click_script="aerospace workspace $sid"

  SLOTS=()
  for i in $(seq 0 $((WORKSPACE_MAX_WINDOWS - 1))); do
    sketchybar --add item "space1.$sid.slot.$i" left \
      --set "space1.$sid.slot.$i" \
        associated_display=1 \
        drawing=off \
        icon.drawing=off \
        label.font="sketchybar-app-font:Regular:16.0" \
        label.padding_left=0 \
        label.padding_right=0
    SLOTS+=("space1.$sid.slot.$i")
  done

  sketchybar --add bracket "space1.$sid" "space1.$sid.num" "${SLOTS[@]}" \
    --set "space1.$sid" \
      background.color="$SPACE_BG" \
      background.corner_radius=5 \
      background.height=22 \
      background.drawing=on

  # Brackets ignore their own padding for spacing, so a plain spacer item is
  # the only way to put a visible gap between two pills.
  sketchybar --add item "space1.$sid.gap" left \
    --set "space1.$sid.gap" associated_display=1 icon.drawing=off label.drawing=off width=8
done


########################################
# EXTERNAL ROW (4–6) → display 2, monitor 1
########################################
SPACE_MON2=("4" "5" "6")

for sid in "${SPACE_MON2[@]}"; do
  sketchybar --add item "space2.$sid.num" left \
    --set "space2.$sid.num" \
      associated_display=2 \
      icon="$sid" \
      icon.padding_left=10 \
      icon.padding_right=10 \
      label.drawing=off \
      click_script="aerospace workspace $sid"

  SLOTS=()
  for i in $(seq 0 $((WORKSPACE_MAX_WINDOWS - 1))); do
    sketchybar --add item "space2.$sid.slot.$i" left \
      --set "space2.$sid.slot.$i" \
        associated_display=2 \
        drawing=off \
        icon.drawing=off \
        label.font="sketchybar-app-font:Regular:16.0" \
        label.padding_left=0 \
        label.padding_right=0
    SLOTS+=("space2.$sid.slot.$i")
  done

  sketchybar --add bracket "space2.$sid" "space2.$sid.num" "${SLOTS[@]}" \
    --set "space2.$sid" \
      background.color="$SPACE_BG" \
      background.corner_radius=5 \
      background.height=22 \
      background.drawing=on

  sketchybar --add item "space2.$sid.gap" left \
    --set "space2.$sid.gap" associated_display=2 icon.drawing=off label.drawing=off width=8
done

# Single invisible controller that updates every workspace pill in one
# script run (see workspace_pills.sh for why this is one item instead of
# one script per pill). update_freq=1 is a cheap polling fallback for
# window *closes*, which AeroSpace has no dedicated event for (unlike
# aerospace_window_change below, used for opens) — without it, closing a
# window in a non-focused workspace wouldn't refresh its pill until the
# next workspace switch.
sketchybar --add item workspace_pills left \
  --set workspace_pills drawing=off icon.drawing=off label.drawing=off \
    update_freq=1 \
    script="$PLUGIN_DIR/workspace_pills.sh" \
  --subscribe workspace_pills aerospace_workspace_change front_app_switched front_app_focus_changed aerospace_window_change


########################################
# RIGHT SIDE – DISPLAY 1 (Laptop)
########################################

sketchybar --add item clock right \
  --set clock \
      associated_display=1 \
      icon= \
      icon.color="$RIGHT_ICON" \
      label.color="$RIGHT_LABEL" \
      update_freq=15 \
      background.color="$SPACE_BG" \
      background.corner_radius=5 \
      background.height=24 \
      background.drawing=on \
      icon.padding_left=10 \
      icon.padding_right=4 \
      label.padding_left=2 \
      label.padding_right=10 \
      script="$PLUGIN_DIR/clock.sh" \
      click_script="$PLUGIN_DIR/clock.sh click"

# sketchybar --add item wifi right \
#   --set wifi \
#       associated_display=1 \
#       icon= \
#       icon.color="$RIGHT_ICON" \
#       label.color="$RIGHT_LABEL" \
#       update_freq=10 \
#       background.color="$SPACE_BG" \
#       background.corner_radius=14 \
#       background.height=24 \
#       background.drawing=on \
#       icon.padding_left=10 \
#       icon.padding_right=4 \
#       label.padding_left=2 \
#       label.padding_right=10 \
#       script="$PLUGIN_DIR/wifi.sh" \
#       click_script="$PLUGIN_DIR/wifi.sh click"

sketchybar --add item battery right \
  --subscribe battery power_source_change \
  --set battery \
      associated_display=1 \
      icon= \
      icon.color="$RIGHT_ICON" \
      label.color="$RIGHT_LABEL" \
      update_freq=30 \
      background.color="$SPACE_BG" \
      background.corner_radius=5 \
      background.height=24 \
      background.drawing=on \
      icon.padding_left=10 \
      icon.padding_right=4 \
      label.padding_left=2 \
      label.padding_right=10 \
      script="$PLUGIN_DIR/battery.sh" \
      click_script="$PLUGIN_DIR/battery.sh click"


########################################
# RIGHT SIDE – DISPLAY 2 (External)
########################################

sketchybar --add item clock_ext right \
  --set clock_ext \
      associated_display=2 \
      icon= \
      icon.color="$RIGHT_ICON" \
      label.color="$RIGHT_LABEL" \
      update_freq=15 \
      background.color="$SPACE_BG" \
      background.corner_radius=5 \
      background.height=24 \
      background.drawing=on \
      icon.padding_left=10 \
      icon.padding_right=4 \
      label.padding_left=2 \
      label.padding_right=10 \
      script="$PLUGIN_DIR/clock.sh" \
      click_script="$PLUGIN_DIR/clock.sh click"

# sketchybar --add item wifi_ext right \
#   --set wifi_ext \
#       associated_display=2 \
#       icon= \
#       icon.color="$RIGHT_ICON" \
#       label.color="$RIGHT_LABEL" \
#       update_freq=10 \
#       background.color="$SPACE_BG" \
#       background.corner_radius=14 \
#       background.height=24 \
#       background.drawing=on \
#       icon.padding_left=10 \
#       icon.padding_right=4 \
#       label.padding_left=2 \
#       label.padding_right=10 \
#       script="$PLUGIN_DIR/wifi.sh" \
#       click_script="$PLUGIN_DIR/wifi.sh click"

sketchybar --add item battery_ext right \
  --subscribe battery_ext power_source_change \
  --set battery_ext \
      associated_display=2 \
      icon= \
      icon.color="$RIGHT_ICON" \
      label.color="$RIGHT_LABEL" \
      update_freq=30 \
      background.color="$SPACE_BG" \
      background.corner_radius=5 \
      background.height=24 \
      background.drawing=on \
      icon.padding_left=10 \
      icon.padding_right=4 \
      label.padding_left=2 \
      label.padding_right=10 \
      script="$PLUGIN_DIR/battery.sh" \
      click_script="$PLUGIN_DIR/battery.sh click"

########################################
# DISPLAY ADAPTATION (1 vs 2+ monitors)
########################################

sketchybar --add item display_adapter right \
  --set display_adapter drawing=off script="$PLUGIN_DIR/display_adapt.sh" \
  --subscribe display_adapter display_change

# Run once at startup
"$PLUGIN_DIR/display_adapt.sh" &

########################################
# INITIAL UPDATE
########################################
sketchybar --update
